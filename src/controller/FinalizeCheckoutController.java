package controller;

public class FinalizeCheckoutController {

	//this controller will depopulate the Orders table for every item connected with the customer's email
	//but first it will gather all of the attributes of the items and send an email to the website
	//a method returns true if the email was sent correctly
	public boolean finalizeOrder(){
		
	}
}
